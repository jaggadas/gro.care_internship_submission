name: flutter_shorts_internship_submission
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.4
  card_swiper: ^2.0.3
  chewie: ^1.3.1
  video_player: ^2.3.0
  http: ^0.13.6
  provider: ^6.0.5
  google_fonts: ^4.0.4
  shimmer: ^2.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true